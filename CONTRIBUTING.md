# Contributing

Use Node.js 20+, run `npm run check` and `npm test` before opening a pull request, and keep changes focused. Do not commit `.bnb-terminal-wallet.json`, key material, screenshots containing addresses, or dependency folders.

For user-visible changes, add or update a test and README guidance. Security-sensitive changes require a threat-model note in the pull request.
